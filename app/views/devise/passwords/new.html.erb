<div class="col-xs-10 col-xs-offset-1 col-sm-8 col-sm-offset-2 col-md-4 col-md-offset-4 box">
  <h2 class="text-center">Reseteo de contraseña</h2>
  <br />

  <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :email, required: true, autofocus: true %>
      <br />
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Enviar instrucciones", class: 'btn btn-primary btn-block' %>
    </div>
  <% end %>
</div>