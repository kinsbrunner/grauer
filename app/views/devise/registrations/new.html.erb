<div class="col-xs-10 col-xs-offset-1 col-sm-8 col-sm-offset-2 col-md-4 col-md-offset-4 box">
  <h2 class="text-center">Registro de Usuario</h2>

  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :email, required: true, autofocus: true %>
      <br />
      <%= f.input :firstname, required: true %>
      <br />
      <%= f.input :lastname, required: true %>
      <br />
      <%= f.input :password, required: true, hint: ("Longitud mínima de #{@minimum_password_length} caracteres" if @minimum_password_length) %>
      <br />
      <%= f.input :password_confirmation, required: true %>
      <br />
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Sign up", class: 'btn btn-primary btn-block' %>
    </div>
  <% end %>
</div>